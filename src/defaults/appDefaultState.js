// Generice state for application
export default {
    // To mark the whole app is busy, thus can not receive input / command
    isBusy: false,
    // App level message
    msg: {
        status: "info",
        msg: "default message"
    }
}
