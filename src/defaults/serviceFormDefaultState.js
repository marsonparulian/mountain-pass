// Default state for Service form

const serviceFormDefaultState = {
    fields: {
        name: {
            value: "Apple"
        },
        version: {
            value: "1.0.0",
        }
    }
}
export default serviceFormDefaultState;
